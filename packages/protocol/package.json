{
	"name": "@tldraw-code/protocol",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "Wire protocol (envelope schema and command catalog) shared by the MCP server and the canvas app.",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"typecheck": "tsc -p tsconfig.json"
	},
	"dependencies": {
		"zod": "^4.6.5"
	}
}
