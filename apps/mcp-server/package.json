{
	"name": "@tldraw-code/mcp-server",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "Local MCP server (stdio) that bridges Claude Code to the tldraw canvas over WebSocket.",
	"scripts": {
		"start": "tsx src/main.ts",
		"build": "tsc -p tsconfig.json",
		"typecheck": "tsc -p tsconfig.json"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.30.1",
		"@tldraw-code/protocol": "workspace:*",
		"ws": "^8.21.3",
		"zod": "^4.6.5"
	},
	"devDependencies": {
		"@types/ws": "^8.18.1"
	}
}
